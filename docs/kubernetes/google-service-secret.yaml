apiVersion: v1
kind: Secret
metadata:
  name: google-service-secret
  namespace: google-authz
type: Opaque
stringData:
  credentials.json: |
    # Replace the content below with your actual Google service account JSON.
    # Example:
    # {
    #   "type": "service_account",
    #   "project_id": "my-project",
    #   ...
    # }
